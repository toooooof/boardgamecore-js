/**
 * Reference is here to declare all static global var/const.
 * It allows to have variables instead of int.
 * Not a mandatory file, but very handy.
 * 
 * Example :
 * 
 * // declaring the players' colors
 * var RED = 0;
 * var GREEN = 1;
 * var YELLOW = 2;
 */