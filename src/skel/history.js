var Log = {

	TECH_LOGS: [],

	displayLogMethod: function(player, action, param) {
		var res = $('<div>');
		if (action == val) {
			res.append(player + " do action");
		} else if (action == other_val) {
			
		}
		return res;
	},

}

